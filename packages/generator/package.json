{
  "name": "@skyface/generator",
  "version": "0.0.1",
  "description": "CLI to generate weather avatar variants using AI",
  "license": "MIT",
  "type": "module",
  "bin": {
    "skyface": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "@skyface/core": "workspace:*",
    "commander": "^12.0.0",
    "ora": "^8.0.0",
    "picocolors": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
